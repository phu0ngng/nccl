stages:
  - build
  - setup
  - health
  - unit_tests
  - api_tests
  - perf_tests_single
  - perf_tests_multi
  - perf_regression
  - replay_tests

variables:
  RUN_MULTI_REGRESSION: "0" # Set to 1 to run multinode regression pipeline
  MULTI_PERFTEST_NODE_COUNT: 4
  CUDA_VERSION: "11.7"
  REGRESSION_BASELINE: "v2.15.5-1" # Compare to stable branch (Update git tag as necessary)
  REGRESSION_THRESHOLD: 7 # What percent difference should we use to compare?
  REGRESSION_ITERATIONS: 5 # How many iterations should we run latency and overhead tests?
  REGRESSION_CHECK_CPU_OVERHEAD: 0 # Should we run CPU overhead tests in the perf regression?
  MULTI_REGRESSION_SALLOC_TIME: 45 # How many minutes to salloc for multinode regression. This can be tuned for multinode pipelines
  SINGLE_REGRESSION_SALLOC_TIME: 30 # How many minutes to salloc for multinode regression. This can be tuned for multinode pipelines
  SHARP_MULTI_JOB_TIME:  10
  RUN_ON_SELENE_SHARP: "0"
  BUILD_TRACE: "0"
  RUN_ON_AWS: "1"
  RUN_ON_EOS: "1"
  RUN_ON_DRACO_OCI: "1"
  RUN_UNIT_API_TESTS_ON_DRACO_OCI: "0" # TODO (kartiki/bewilliams) - Enable after addressing https://nvbugspro.nvidia.com/bug/4861033

# Run our CI on:
# - Every commit to master
# - Every commit to a merge request
# - Schedules (nightlies and weeklies)
# - Merge requests without "Draft:" in the title
workflow:
  rules:
    - if: $CI_MERGE_REQUEST_TITLE !~ /^Draft:/ && ($CI_PIPELINE_SOURCE == 'merge_request_event' || $CI_PIPELINE_SOURCE == 'schedule' || $CI_PIPELINE_SOURCE == 'web' || $CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH == "next" || $CI_COMMIT_BRANCH == "stable" || $CI_COMMIT_BRANCH == $NCCL_RELEASE_BRANCH)

build:
  stage: build
  rules:
    - when: always
  tags:
    - build
  variables:
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    GIT_CLONE_PATH: "$CI_BUILDS_DIR/$CI_COMMIT_SHA"
  script:
    - echo "Using CUDA_HOME=$CUDA_HOME, MPI_HOME=$MPI_HOME"
    - make -j test.build MPI=1 MPI_HOME=$MPI_HOME CUDA_HOME=$CUDA_HOME NVCC_GENCODE="-gencode=arch=compute_60,code=sm_60 -gencode=arch=compute_70,code=sm_70 -gencode=arch=compute_80,code=sm_80"
  artifacts:
    paths:
      - build
    expire_in: 4 hrs
  interruptible: true

build-trace:
  stage: build
  rules:
    - if: $BUILD_TRACE == "1" || $CI_PIPELINE_SOURCE == 'schedule'
      when: always
    - when: never
  tags:
    - build
  variables:
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
  script:
    - echo "Using CUDA_HOME=$CUDA_HOME, MPI_HOME=$MPI_HOME"
    - make -j test.build MPI=1 TRACE=1 MPI_HOME=$MPI_HOME CUDA_HOME=$CUDA_HOME
  interruptible: true

build-eos-aws:
  stage: build
  rules:
    - if: $RUN_ON_EOS == "1" || $RUN_ON_AWS == "1"
      when: always
    - when: never
  tags:
    - build-with-docker
  script:
    # EOS cluster info
    #   NVIDIA-SMI 535.129.03  Driver Version: 535.129.03  CUDA Version: 12.2
    #   NVCC_GENCODE: 90
    #   OS: ubuntu22.04
    #   MPI_HOME: "/usr/mpi/gcc/openmpi-4.1.5rc2"
    - docker/make.sh eos
  artifacts:
    paths:
      - build
    exclude:
      - build/**/*.o
    expire_in: 4 hrs
  interruptible: true

build-draco_oci:
  stage: build
  rules:
    - if: $RUN_ON_DRACO_OCI == "1"
      when: always
    - when: never
  tags:
    - build-with-docker
  script:
    # Draco OCI-IAD cluster info
    #   NVIDIA-SMI 535.113.01  Driver Version: 535.104.12  CUDA Version: 12.2
    #   NVCC_GENCODE: 80
    #   OS: ubuntu20.04
    #   MPI_HOME: "/lustre/fsw/portfolios/coreai/projects/coreai_libraries_nccl/local/openmpi-4.1.4"
    - docker/make.sh draco-oci
  artifacts:
    paths:
      - build
    exclude:
      - build/**/*.o
    expire_in: 4 hrs
  interruptible: true

setup-eos:
  stage: unit_tests
  rules:
    - if: $RUN_ON_EOS == "1"
      when: always
    - when: never
  tags:
    - nccl-eos
  script:
    - nccl/test/scripts/gitlab-runner-fairshare.sh
  interruptible: true

setup-draco-oci:
  stage: unit_tests
  rules:
    - if: $RUN_ON_DRACO_OCI == "1"
      when: always
    - when: never
  tags:
    - nccl-eos
  script:
    - nccl/test/scripts/gitlab-runner-fairshare.sh
  interruptible: true

setup-AWS_H100_EFA:
  stage: setup
  rules:
    - if: $RUN_ON_AWS == "1"
      when: always
    - when: never
  tags:
    - aws_p5
  needs:
    # Builds targetted for EOS work on AWS as well as the build config is the same across both clusters.
    # TODO - Refactor build jobs to decouple EOS and AWS.
    - build-eos-aws
  variables:
    AWS_HEAD_NODE: aws-pcluster-head
    AWS_HEAD_NODE_WORKDIR: /shared/gitlab-runner/nccl-ci/${CI_COMMIT_SHORT_SHA}
  script:
    - rsync -a --exclude ./.git ./ $AWS_HEAD_NODE:$AWS_HEAD_NODE_WORKDIR/

health-P100:
  stage: health
  rules:
    - when: always
  needs: []
  tags:
    - p100
  script:
    - srun --exclusive -t 00:02:00 -n 1 -c 8 -pP100 ./test/scripts/gitlab-runner-health-check.sh
  interruptible: true

health-P40:
  stage: health
  rules:
    - when: always
  needs: []
  tags:
    - p40
  script:
    - srun --exclusive -t 00:02:00 -n 1 -c 8 -pP40 ./test/scripts/gitlab-runner-health-check.sh
  interruptible: true

health-GP100:
  stage: health
  rules:
    - when: always
  needs: []
  tags:
    - gp100
  script:
    - srun --exclusive -t 00:02:00 -n 1 -c 8 -pGP100 ./test/scripts/gitlab-runner-health-check.sh
  interruptible: true

health-A40:
  stage: health
  rules:
    - when: always
  needs: []
  tags:
    - a40
  script:
    - srun --exclusive -t 00:02:00 -N 2 -c 2 -pA40 ./test/scripts/gitlab-runner-health-check.sh
  interruptible: true

health-A100:
  stage: health
  rules:
    - when: always
  needs: []
  tags:
    - a100pci
  script:
    - srun --exclusive -t 00:02:00 -n 1 -c 8 -pA100 ./test/scripts/gitlab-runner-health-check.sh
  interruptible: true

unit_tests-P100:
  stage: unit_tests
  rules:
    - when: always
  needs: ["build","health-P100"]
  tags:
    - p100
  variables:
    NCCL_IB_HCA: "mlx5_0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 8 -pP100"
  script:
    - ./test/scripts/gitlab-runner-unit.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

unit_tests-P40:
  stage: unit_tests
  rules:
    - when: always
  needs: ["build","health-P40"]
  tags:
    - p40
  variables:
    NCCL_IB_HCA: "mlx5_0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 8 -pP40"
  script:
    - ./test/scripts/gitlab-runner-unit.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

unit_tests-GP100:
  stage: unit_tests
  rules:
    - when: always
  needs: ["build","health-GP100"]
  tags:
    - gp100
  variables:
    NCCL_CUMEM_ENABLE: "0"
    NCCL_IB_HCA: "mlx5_0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 8 -pGP100"
  script:
    - ./test/scripts/gitlab-runner-unit.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

unit_tests-A40-single:
  stage: unit_tests
  rules:
    - when: always
  needs: ["build","health-A40"]
  tags:
    - a40
  variables:
    NCCL_IB_HCA: "mlx5_0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 1 -pA40"
  script:
    - ./test/scripts/gitlab-runner-unit.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

unit_tests-A40-dual:
  stage: unit_tests
  rules:
    - when: always
  needs: ["build","health-A40"]
  tags:
    - a40
  variables:
    NCCL_IB_HCA: "mlx5_0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 2 -pA40"
  script:
    - ./test/scripts/gitlab-runner-unit.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

unit_tests-A100:
  stage: unit_tests
  rules:
    - when: always
  needs: ["build","health-A100"]
  tags:
    - a100pci
  variables:
    NCCL_IB_HCA: "mlx5_0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 8 -pA100"
  script:
    - ./test/scripts/gitlab-runner-unit.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

unit_tests-eos:
  stage: unit_tests
  rules:
    - if: $RUN_ON_EOS == "1"
      when: always
    - when: never
  needs: ["build-eos-aws"]
  tags:
    - nccl-eos
  variables:
    DISABLE_MEMLEAK: "1" # Enable after addressing https://nvbugspro.nvidia.com/bug/4868411
    SRUN: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - source docker/eos-config.sh && export CUDA_HOME=$(get_cuda_home) && SLURM_ACCOUNT=$(get_slurm_account)
    - srun --exclusive -n 1 -c8 --account=$SLURM_ACCOUNT -J "${SLURM_ACCOUNT}-cicd::$GITLAB_USER_LOGIN:unit::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t 00:10:00 ./test/scripts/gitlab-runner-unit.sh
  interruptible: true

unit_tests-AWS_H100_EFA:
  stage: unit_tests
  rules:
    - if: $RUN_ON_AWS == "1"
      when: always
    - when: never
  needs:
    - setup-AWS_H100_EFA
  tags:
    - aws_p5
  variables:
    GIT_STRATEGY: none
    AWS_HEAD_NODE: aws-pcluster-head
    AWS_HEAD_NODE_WORKDIR: /shared/gitlab-runner/nccl-ci/${CI_COMMIT_SHORT_SHA}
    RUN_SCRIPT: run_${CI_JOB_NAME}.sh
    TEST_CMD: ./test/scripts/gitlab-runner-unit.sh
  before_script:
    - |
      cat > $RUN_SCRIPT << EOF
      source docker/aws-config.sh && configure_aws_test_env

      export SRUN="srun --exclusive -t 00:10:00 -n 1 -G 8 -p compute"

      $TEST_CMD
      EOF
    - rsync $RUN_SCRIPT $AWS_HEAD_NODE:$AWS_HEAD_NODE_WORKDIR
  script:
    - ssh $AWS_HEAD_NODE "cd $AWS_HEAD_NODE_WORKDIR && bash -l $RUN_SCRIPT"
  artifacts:
    when: always
    paths:
      - $RUN_SCRIPT
    expire_in: 4 hrs

unit_tests-draco_oci:
  stage: unit_tests
  rules:
    - if: $RUN_ON_DRACO_OCI == "1" && $RUN_UNIT_API_TESTS_ON_DRACO_OCI == "1"
      when: always
    - when: never
  needs:
    - build-draco_oci
  tags:
    - draco-oci-iad
  variables:
    NNODES: 1
    NGPUS: 8
    SRUN: "" # Needs to be empty for CI runner scripts performing cluster jobs
    TEST_CMD: ./test/scripts/gitlab-runner-unit.sh
    TIMEOUT: "00:10:00" # HH:MM:SS
  script:
    - source docker/draco-oci-config.sh && configure_test_env
    - srun --exclusive --nodes $NNODES --gpus-per-node $NGPUS --account=$SLURM_ACCOUNT -J "$SLURM_ACCOUNT-cicd::${GITLAB_USER_LOGIN}::unit::${CI_COMMIT_BRANCH}:${CI_COMMIT_SHORT_SHA}" -t $TIMEOUT $TEST_CMD
  interruptible: true

apitest-P100:
  stage: api_tests
  rules:
    - when: always
  needs: ["build", "unit_tests-P100"]
  tags:
    - p100
  variables:
    NCCL_IB_HCA: "mlx5_0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 8 -pP100"
  script:
    - ./test/scripts/gitlab-runner-apitest.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

apitest-P40:
  stage: api_tests
  rules:
    - when: always
  needs: ["build", "unit_tests-P40"]
  tags:
    - p40
  variables:
    NCCL_IB_HCA: "mlx5_0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 8 -pP40"
  script:
    - ./test/scripts/gitlab-runner-apitest.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

apitest-GP100:
  stage: api_tests
  rules:
    - when: always
  needs: ["build", "unit_tests-GP100"]
  tags:
    - gp100
  variables:
    NCCL_CUMEM_ENABLE: "0"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 8 -pGP100"
  script:
    - ./test/scripts/gitlab-runner-apitest.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true
 
apitest-A40:
  stage: api_tests
  rules:
    - when: always
  needs: ["build", "unit_tests-A40-single", "unit_tests-A40-dual"]
  tags:
    - a40
  variables:
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 2 -pA40"
  script:
    - ./test/scripts/gitlab-runner-apitest.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

apitest-A100:
  stage: api_tests
  rules:
    - when: always
  needs: ["build", "unit_tests-A100"]
  tags:
    - a100pci
  variables:
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SRUN: "srun --exclusive -t 00:10:00 -n 1 -c 8 -pA100"
  script:
    - ./test/scripts/gitlab-runner-apitest.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

apitest-DGX-1V:
  stage: api_tests
  rules:
    - if: $RUN_ON_DRACO == "1"
      when: always
    - when: never
  needs: [build]
  tags:
    - dgx-1v
  variables:
    CUDA_HOME: "/gpfs/fs1/projects/sw_gpucomms/users/svc_nvshmem_ci/cuda/cuda-${CUDA_VERSION}"
    PARTITION_LIST: "batch_short_dgx1_m2"
    SRUN: "" # Needs to be empty for CI runner scripts performing cluster jobs
    SRUN_PARAMS: "--exclusive -N 1 -n 1 -G 8 -c 8 --account=coreai_libraries_nccl --nv-meta ml-model.nccl -t 00:20:00"
  script:
    - srun $SRUN_PARAMS -p $PARTITION_LIST ./test/scripts/gitlab-runner-apitest.sh minimal
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

apitest-DGX-2:
  stage: api_tests
  rules:
    - if: $RUN_ON_DRACO == "1"
      when: always
    - when: never
  needs: [build]
  tags:
    - dgx-2
  variables:
    CUDA_HOME: "/gpfs/fs1/projects/sw_gpucomms/users/svc_nvshmem_ci/cuda/cuda-${CUDA_VERSION}"
    PARTITION_LIST: "batch_short_dgx2h_m2"
    SRUN: "" # Needs to be empty for CI runner scripts performing cluster jobs
    SRUN_PARAMS: "--exclusive --mem=0 -N 1 -n 1 -G 16 -c 16 --account=coreai_libraries_nccl --nv-meta ml-model.nccl -t 00:20:00"
  script:
    - srun $SRUN_PARAMS -p $PARTITION_LIST ./test/scripts/gitlab-runner-apitest.sh minimal
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

apitest-DGX-A100:
  stage: api_tests
  rules:
    - if: $RUN_ON_SELENE == "1" && $CUDA_VERSION != "10.2"
      when: always
    - when: never
  needs: [build]
  tags:
    - dgx-a100
  variables:
    CUDA_HOME: "/project/nccl/toolkits/cuda-${CUDA_VERSION}/"
    NCCL_SOCKET_IFNAME: "ibp12s0"
    NCCL_CUMEM_ENABLE: "1"
    SRUN: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - srun -n 1 --account=coreai_libraries_nccl -J "coreai_libraries_nccl-cicd::$GITLAB_USER_LOGIN:apitest::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -pluna -t 00:30:00 ./test/scripts/gitlab-runner-apitest.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

apitest-eos:
  stage: api_tests
  rules:
    - if: $RUN_ON_EOS == "1"
      when: always
    - when: never
  needs: ["build-eos-aws"]
  tags:
    - nccl-eos
  variables:
    SRUN: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - source docker/eos-config.sh && export CUDA_HOME=$(get_cuda_home) && SLURM_ACCOUNT=$(get_slurm_account)
    - srun -n 1 --account=$SLURM_ACCOUNT -J "${SLURM_ACCOUNT}-cicd::$GITLAB_USER_LOGIN:apitest::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t 00:10:00 ./test/scripts/gitlab-runner-apitest.sh
  interruptible: true

apitest-AWS_H100_EFA:
  stage: api_tests
  rules:
    - if: $RUN_ON_AWS == "1"
      when: always
    - when: never
  needs:
    - setup-AWS_H100_EFA
  tags:
    - aws_p5
  variables:
    GIT_STRATEGY: none
    AWS_HEAD_NODE: "aws-pcluster-head"
    AWS_HEAD_NODE_WORKDIR: "/shared/gitlab-runner/nccl-ci/${CI_COMMIT_SHORT_SHA}"
    RUN_SCRIPT: run_${CI_JOB_NAME}.sh
    TEST_CMD: ./test/scripts/gitlab-runner-apitest.sh
  before_script:
    - |
      cat > $RUN_SCRIPT << EOF
      source docker/aws-config.sh && configure_aws_test_env

      export SRUN="srun --exclusive -t 00:30:00 -n 1 -G 8 -p compute"

      $TEST_CMD
      EOF
    - rsync $RUN_SCRIPT $AWS_HEAD_NODE:$AWS_HEAD_NODE_WORKDIR
  script:
    - ssh $AWS_HEAD_NODE "cd $AWS_HEAD_NODE_WORKDIR && bash -l $RUN_SCRIPT"
  artifacts:
    when: always
    paths:
      - $RUN_SCRIPT
    expire_in: 4 hrs

apitest-draco_oci:
  stage: api_tests
  rules:
    - if: $RUN_ON_DRACO_OCI == "1" && $RUN_UNIT_API_TESTS_ON_DRACO_OCI == "1"
      when: always
    - when: never
  needs:
    - build-draco_oci
    - unit_tests-draco_oci
  tags:
    - draco-oci-iad
  variables:
    NNODES: 1
    NGPUS: 8
    SRUN: "" # Needs to be empty for CI runner scripts performing cluster jobs
    TEST_CMD: ./test/scripts/gitlab-runner-apitest.sh
    TIMEOUT: "00:30:00" # HH:MM:SS
  script:
    - source docker/draco-oci-config.sh && configure_test_env
    - srun --exclusive --nodes $NNODES --gpus-per-node $NGPUS --account=$SLURM_ACCOUNT -J "$SLURM_ACCOUNT-cicd::${GITLAB_USER_LOGIN}::apitest::${CI_COMMIT_BRANCH}:${CI_COMMIT_SHORT_SHA}" -t $TIMEOUT $TEST_CMD
  interruptible: true

perftest-P100:
  stage: perf_tests_single
  rules:
    - when: always
  needs: [build, apitest-P100]
  tags:
    - p100
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    NNODES: 1
    NGPUS: 8
    SALLOC: "salloc --exclusive -t 00:10:00 -N ${NNODES} -n ${NGPUS} -pP100"
  script:
    - ./test/scripts/gitlab-runner-perftest.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-P40:
  stage: perf_tests_single
  rules:
    - when: always
  needs: [build, apitest-P40]
  tags:
    - p40
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    NNODES: 1
    NGPUS: 8
    SALLOC: "salloc --exclusive -t 00:10:00 -N ${NNODES} -n ${NGPUS} -pP40"
  script:
    - ./test/scripts/gitlab-runner-perftest.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-GP100:
  stage: perf_tests_single
  rules:
    - when: always
  needs: [build, apitest-GP100]
  tags:
    - gp100
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    NNODES: 1
    NGPUS: 8
    SALLOC: "salloc --exclusive -t 00:10:00 -N ${NNODES} -n ${NGPUS} -pGP100"
  script:
    - ./test/scripts/gitlab-runner-perftest.sh 1G 2
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-A40:
  stage: perf_tests_single
  rules:
    - when: always
  needs: [build, apitest-A40]
  tags:
    - a40
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    NNODES: 2
    NGPUS: 2
    SALLOC: "salloc --exclusive -t 00:10:00 -N ${NNODES} -n ${NGPUS} -pA40"
  script:
    - ./test/scripts/gitlab-runner-perftest.sh 1G 0
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-A100:
  stage: perf_tests_single
  rules:
    - when: always
  needs: [build, apitest-A100]
  tags:
    - a100pci
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    NNODES: 1
    NGPUS: 8
    SALLOC: "salloc --exclusive -t 00:10:00 -N ${NNODES} -n ${NGPUS} -pA100"
  script:
    - ./test/scripts/gitlab-runner-perftest.sh 1G 0
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-DGX-1V:
  stage: perf_tests_single
  rules:
    - if: $RUN_ON_DRACO == "1"
      when: always
    - when: never
  needs: [build, apitest-DGX-1V]
  tags:
    - dgx-1v
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/home/svc_nvshmem_ci/mpi/openmpi-1.10.7"
    CUDA_HOME: "/gpfs/fs1/projects/sw_gpucomms/users/svc_nvshmem_ci/cuda/cuda-${CUDA_VERSION}"
    PARTITION_LIST: "batch_short_dgx1_m2"
    NNODES: 1
    NGPUS: 8
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - salloc --gpus-per-node=${NGPUS} --ntasks-per-node=${NGPUS} -N ${NNODES} --account=coreai_libraries_nccl --nv-meta ml-model.nccl -p $PARTITION_LIST -t 00:25:00 ./test/scripts/gitlab-runner-perftest.sh 2G
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-DGX-2:
  stage: perf_tests_single
  rules:
    - if: $RUN_ON_DRACO == "1"
      when: always
    - when: never
  needs: [build, apitest-DGX-2]
  tags:
    - dgx-2
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/home/svc_nvshmem_ci/mpi/openmpi-1.10.7"
    CUDA_HOME: "/gpfs/fs1/projects/sw_gpucomms/users/svc_nvshmem_ci/cuda/cuda-${CUDA_VERSION}"
    PARTITION_LIST: "batch_short_dgx2h_m2"
    NNODES: 1
    NGPUS: 16
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - salloc -N 1 --gpus-per-node=${NGPUS} --ntasks-per-node=${NGPUS} --account=coreai_libraries_nccl --nv-meta ml-model.nccl -p $PARTITION_LIST -t 00:35:00 ./test/scripts/gitlab-runner-perftest.sh 1G
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-DGX-A100:
  stage: perf_tests_single
  rules:
    - if: $RUN_ON_SELENE == "1" && $CUDA_VERSION != "10.2"
      when: always
    - when: never
  needs: [build, apitest-DGX-A100]
  tags:
    - dgx-a100
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/home/svcnccl/install/openmpi-1.10.7"
    CUDA_HOME: "/project/nccl/toolkits/cuda-${CUDA_VERSION}/"
    NCCL_CUMEM_ENABLE: "1"
    NNODES: 1
    NGPUS: 8
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - salloc -N ${NNODES} -n ${NGPUS} --account=coreai_libraries_nccl -pluna -J "coreai_libraries_nccl-cicd::$GITLAB_USER_LOGIN:perftest-single::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t 00:30:00 ./test/scripts/gitlab-runner-perftest.sh 16G
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-single-eos:
  stage: perf_tests_single
  rules:
    - if: $RUN_ON_EOS == "1"
      when: always
    - when: never
  needs: ["build-eos-aws"]
  tags:
    - nccl-eos
  variables:
    NCCL_DEBUG: "WARN"
    MPI_PARAMS: "-mca btl tcp,self"
    NNODES: 1
    NGPUS: 8
    SKIP_FT: 1
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - source docker/eos-config.sh && export CUDA_HOME=$(get_cuda_home) && export MPI_HOME=$(get_openmpi_home) && SLURM_ACCOUNT=$(get_slurm_account)
    - salloc -N ${NNODES} -n ${NGPUS} --account=$SLURM_ACCOUNT -J "${SLURM_ACCOUNT}-cicd::$GITLAB_USER_LOGIN:perftest-single::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t 00:20:00 ./test/scripts/gitlab-runner-perftest.sh 16G
  interruptible: true

perftest_single-AWS_H100_EFA:
  stage: perf_tests_single
  rules:
    - if: $RUN_ON_AWS == "1"
      when: always
    - when: never
  needs:
    - setup-AWS_H100_EFA
  tags:
    - aws_p5
  variables:
    GIT_STRATEGY: none
    AWS_HEAD_NODE: "aws-pcluster-head"
    AWS_HEAD_NODE_WORKDIR: "/shared/gitlab-runner/nccl-ci/${CI_COMMIT_SHORT_SHA}"
    NNODES: 1
    NGPUS: 8
    RUN_SCRIPT: run_${CI_JOB_NAME}.sh
    TEST_CMD: ./test/scripts/gitlab-runner-perftest.sh
  before_script:
    - |
      cat > $RUN_SCRIPT << EOF
      source docker/aws-config.sh && configure_aws_test_env

      export MPI_PARAMS="--oversubscribe --mca btl tcp,self --mca btl_tcp_if_exclude lo,docker0 --bind-to none"
      export NNODES=$NNODES
      export NGPUS=$NGPUS
      export SALLOC="salloc --exclusive -t 00:10:00 -N $NNODES -n $NGPUS -p compute"
      export SKIP_FT=1
      export UCX_TLS=tcp

      $TEST_CMD
      EOF
    - rsync $RUN_SCRIPT $AWS_HEAD_NODE:$AWS_HEAD_NODE_WORKDIR
  script:
    - ssh $AWS_HEAD_NODE "cd $AWS_HEAD_NODE_WORKDIR && bash -l $RUN_SCRIPT"
  artifacts:
    when: always
    paths:
      - $RUN_SCRIPT
    expire_in: 4 hrs

perftest_single-draco_oci:
  stage: perf_tests_single
  rules:
    - if: $RUN_ON_DRACO_OCI == "1"
      when: always
    - when: never
  needs:
    - build-draco_oci
  tags:
    - draco-oci-iad
  variables:
    MPI_PARAMS: "-mca btl tcp,self"
    NNODES: 1
    NGPUS: 8
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
    TEST_CMD: ./test/scripts/gitlab-runner-perftest.sh 16G
    TIMEOUT: "00:20:00" # HH:MM:SS
  script:
    - source docker/draco-oci-config.sh && configure_test_env
    - salloc --exclusive --nodes $NNODES --gpus-per-node $NGPUS --ntasks-per-node $NGPUS --account=$SLURM_ACCOUNT -J "$SLURM_ACCOUNT-cicd::${GITLAB_USER_LOGIN}::perftest-single::${CI_COMMIT_BRANCH}:${CI_COMMIT_SHORT_SHA}" -t $TIMEOUT $TEST_CMD
  interruptible: true

perftest-multi-gc:
  stage: perf_tests_multi
  rules:
    - when: always
  needs: [build, perftest-P100]
  tags:
    - p100
  variables:
    NCCL_IB_HCA: "mlx5_0"
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    NCCL_ALLOC_P2P_NET_LL_BUFFERS: "1" # Add coverage of dedicated P2P LL buffers on constrained rank count
    NNODES: 2
    NGPUS: 8
    SKIP_MULTI_GPU: 1
    SALLOC: "salloc --exclusive -t 00:10:00 -N ${NNODES} --ntasks-per-node=${NGPUS} -w gc[01-02]"
  script:
    - ./test/scripts/gitlab-runner-perftest.sh 128M 0
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-sharp-gc:
  stage: perf_tests_multi
  rules:
    - when: always
  needs: [build, perftest-P100]
  tags:
    - p100
  variables:
    NCCL_IB_HCA: "mlx5_0"
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SHARP_HOME: "/opt/hpcx/v2.7/sharp"
    PLUGIN_PATH: "/opt/nccl-rdma-sharp-plugins/src/.libs"
    NNODES: 2
    NGPUS: 8
    SALLOC: "salloc --exclusive -t 00:10:00 -N ${NNODES} --ntasks-per-node=${NGPUS} -w gc[01-02]"
  script:
    - ./test/scripts/gitlab-runner-perftest-sharp.sh 128M 0
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

perftest-multi-DGX-1V:
  stage: perf_tests_multi
  rules:
    - when: always
  rules:
    - if: $RUN_ON_DRACO == "1"
      when: always
    - when: never
  needs: [build, perftest-DGX-1V]
  tags:
    - dgx-1v
  variables:
    MPI_HOME: "/home/svc_nvshmem_ci/mpi/openmpi-1.10.7"
    CUDA_HOME: "/gpfs/fs1/projects/sw_gpucomms/users/svc_nvshmem_ci/cuda/cuda-${CUDA_VERSION}"
    PARTITION_LIST: "batch_short_dgx1_m2"
    UCX_TLS: "tcp"
    NCCL_DEBUG_SUBSYS: "ENV"
    NCCL_IBEXT_DISABLE: "1"
    NNODES: $MULTI_PERFTEST_NODE_COUNT
    NGPUS: 8
    MPI_PARAMS: "-mca btl tcp,self -mca btl_tcp_if_include enp1s0f0"
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
    DRACO_SALLOC: "--exclusive --gpus-per-node=8 --ntasks-per-node=8 --account=coreai_libraries_nccl --nv-meta ml-model.nccl --exclusive -t 00:25:00"
  script:
    - let NUM_PROCESSES=$MULTI_PERFTEST_NODE_COUNT*8 && salloc $DRACO_SALLOC -N $MULTI_PERFTEST_NODE_COUNT -n $NUM_PROCESSES -G $NUM_PROCESSES -p $PARTITION_LIST ./test/scripts/gitlab-runner-perftest.sh 1G
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true

# Disabled - Allocation timeouts cause too much noise
# perftest-multi-dgx-2:
#   stage: perf_tests_multi
#   rules:
#     - when: always
#   rules:
#     - if: $RUN_ON_DRACO == "1"
#       when: always
#     - when: never
#   needs: [build, unit_tests, perftest-DGX-2]
#   tags:
#     - dgx-2
#   variables:
#     MPI_HOME: "/home/svc_nvshmem_ci/mpi/openmpi-1.10.7"
#     CUDA_HOME: "/gpfs/fs1/projects/sw_gpucomms/users/svc_nvshmem_ci/cuda/cuda-${CUDA_VERSION}"
#     PARTITION_LIST: "batch_short_dgx2h_m2"
#     UCX_TLS: "tcp"
#     NCCL_DEBUG_SUBSYS: "ENV" 
#     NCCL_DEBUG: "INFO"
#     NCCL_IBEXT_DISABLE: "1"
#     MPI_PARAMS: "-mca btl tcp,self -mca btl_tcp_if_include enp134s0f0np0"
#     SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
#     DRACO_SALLOC: "--exclusive --gpus-per-node=16 --ntasks-per-node=16 --account=coreai_libraries_nccl --nv-meta ml-model.nccl --exclusive -t 00:30:00"
#   script:
#     - let NUM_PROCESSES=$MULTI_PERFTEST_NODE_COUNT*16 && salloc $DRACO_SALLOC -N $MULTI_PERFTEST_NODE_COUNT -n $NUM_PROCESSES -G $NUM_PROCESSES -p $PARTITION_LIST ./test/scripts/gitlab-runner-perftest.sh 1G
#   interruptible: true

perftest-multi-selene:
  stage: perf_tests_multi
  rules:
    - if: $RUN_ON_SELENE == "1" && $CUDA_VERSION != "10.2"
      when: always
    - when: never
  needs: [build, perftest-DGX-A100]
  tags:
    - dgx-a100
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/home/svcnccl/install/openmpi-1.10.7"
    CUDA_HOME: "/project/nccl/toolkits/cuda-${CUDA_VERSION}/"
    NCCL_IB_SL: "1"
    OMPI_MCA_btl: "^openib"
    NNODES: $MULTI_PERFTEST_NODE_COUNT
    NGPUS: 8
    OMPI_MCA_btl_tcp_if_include: "enp97s0f1"
    NCCL_CUMEM_ENABLE: "1"
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - let NUM_PROCESSES=$MULTI_PERFTEST_NODE_COUNT*8 && salloc -N $MULTI_PERFTEST_NODE_COUNT -n $NUM_PROCESSES --account=coreai_libraries_nccl -pluna -J "coreai_libraries_nccl-cicd::$GITLAB_USER_LOGIN:perftest-multi::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t 00:20:00 ./test/scripts/gitlab-runner-perftest.sh 16G 0
  interruptible: true

perftest-multi-eos:
  stage: perf_tests_multi
  rules:
    - if: $RUN_ON_EOS == "1"
      when: always
    - when: never
  needs: ["build-eos-aws"]
  tags:
    - nccl-eos
  variables:
    NCCL_IB_SL: "1"
    OMPI_MCA_btl: "^openib"
    MPI_PARAMS: "--oversubscribe --mca btl tcp,self --mca btl_tcp_if_include eth3"
    NNODES: $MULTI_PERFTEST_NODE_COUNT
    NGPUS: 8
    SKIP_FT: 1
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - source docker/eos-config.sh && export CUDA_HOME=$(get_cuda_home) && export MPI_HOME=$(get_openmpi_home) && SLURM_ACCOUNT=$(get_slurm_account)
    - let NUM_PROCESSES=$MULTI_PERFTEST_NODE_COUNT*8 && salloc -N $MULTI_PERFTEST_NODE_COUNT -n $NUM_PROCESSES --account=$SLURM_ACCOUNT -J "${SLURM_ACCOUNT}-cicd::$GITLAB_USER_LOGIN:perftest-multi::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t 00:20:00 ./test/scripts/gitlab-runner-perftest.sh 16G 0
  interruptible: true

perftest-sharp-selene:
  stage: perf_tests_multi
  rules:
    - if: $RUN_ON_SELENE_SHARP == "1" && $CUDA_VERSION != "10.2"
      when: always
    - when: never
  needs: [build, perftest-DGX-A100]
  tags:
    - dgx-a100
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/home/svcnccl/install/openmpi-1.10.7"
    CUDA_HOME: "/project/nccl/toolkits/cuda-${CUDA_VERSION}/"
    SHARP_HOME: "/lustre/fsw/nccl/hpcx/hpcx-v2.14-gcc-MLNX_OFED_LINUX-5-ubuntu20.04-cuda11-gdrcopy2-nccl2.16-x86_64/sharp"
    HPCX_UCX_LIB: "/lustre/fsw/nccl/hpcx/hpcx-v2.14-gcc-MLNX_OFED_LINUX-5-ubuntu20.04-cuda11-gdrcopy2-nccl2.16-x86_64/ucx/lib"
    PLUGIN_PATH: "/lustre/fsw/nccl/hpcx/hpcx-v2.14-gcc-MLNX_OFED_LINUX-5-ubuntu20.04-cuda11-gdrcopy2-nccl2.16-x86_64/nccl_rdma_sharp_plugin/lib"
    NCCL_IB_SL: "1"
    OMPI_MCA_btl: "^openib"
    NNODES: $MULTI_PERFTEST_NODE_COUNT
    NGPUS: 8
    OMPI_MCA_btl_tcp_if_include: "enp97s0f1"
    NCCL_CUMEM_ENABLE: "1"
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
  script:
    - let NUM_PROCESSES=$MULTI_PERFTEST_NODE_COUNT*8 && salloc -N $MULTI_PERFTEST_NODE_COUNT -n $NUM_PROCESSES --account=coreai_libraries_nccl -pluna --network=sharp -J "coreai_libraries_nccl-cicd::$GITLAB_USER_LOGIN:perftest-multi::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t $SHARP_MULTI_JOB_TIME ./test/scripts/gitlab-runner-perftest-sharp.sh 4G 0
  interruptible: true

perftest_multi-AWS_H100_EFA:
  stage: perf_tests_multi
  rules:
    - if: $RUN_ON_AWS == "1"
      when: always
    - when: never
  needs:
    - setup-AWS_H100_EFA
  tags:
    - aws_p5
  variables:
    GIT_STRATEGY: none
    AWS_HEAD_NODE: "aws-pcluster-head"
    AWS_HEAD_NODE_WORKDIR: "/shared/gitlab-runner/nccl-ci/${CI_COMMIT_SHORT_SHA}"
    NNODES: $MULTI_PERFTEST_NODE_COUNT
    NGPUS: 8
    RUN_SCRIPT: run_${CI_JOB_NAME}.sh
    TEST_CMD: ./test/scripts/gitlab-runner-perftest.sh 128M 0
  before_script:
    - |
      cat > $RUN_SCRIPT << EOF
      source docker/aws-config.sh && configure_aws_test_env

      export MPI_PARAMS="--oversubscribe --mca btl tcp,self --mca btl_tcp_if_exclude lo,docker0 --bind-to none"
      export NNODES=$NNODES
      export NGPUS=$NGPUS
      export SALLOC="salloc --exclusive -t 00:10:00 -N $NNODES --ntasks-per-node=$NGPUS -p compute"
      export SKIP_FT=1
      export UCX_TLS=tcp

      $TEST_CMD
      EOF
    - rsync $RUN_SCRIPT $AWS_HEAD_NODE:$AWS_HEAD_NODE_WORKDIR
  script:
    - ssh $AWS_HEAD_NODE "cd $AWS_HEAD_NODE_WORKDIR && bash -l $RUN_SCRIPT"
  interruptible: true
  artifacts:
    when: always
    paths:
      - $RUN_SCRIPT
    expire_in: 4 hrs

perftest_multi-draco_oci:
  stage: perf_tests_multi
  rules:
    - if: $RUN_ON_DRACO_OCI == "1"
      when: always
    - when: never
  needs:
    - build-draco_oci
    - perftest_single-draco_oci
  tags:
    - draco-oci-iad
  variables:
    MPI_PARAMS: "-mca btl tcp,self"
    NNODES: $MULTI_PERFTEST_NODE_COUNT
    NGPUS: 8
    SALLOC: "" # Needs to be empty for CI runner scripts performing cluster jobs
    TEST_CMD: ./test/scripts/gitlab-runner-perftest.sh 128M 0
    TIMEOUT: "00:20:00" # HH:MM:SS
  script:
    - source docker/draco-oci-config.sh && configure_test_env
    - salloc --exclusive --nodes $NNODES --gpus-per-node $NGPUS --ntasks-per-node $NGPUS --account=$SLURM_ACCOUNT -J "$SLURM_ACCOUNT-cicd::${GITLAB_USER_LOGIN}::perftest-single::${CI_COMMIT_BRANCH}:${CI_COMMIT_SHORT_SHA}" -t $TIMEOUT $TEST_CMD
  interruptible: true

perf-regression-single-DGX-A100:
  stage: perf_regression
  rules:
    - if: $RUN_ON_SELENE == "1" && $CUDA_VERSION != "10.2" && $CI_PIPELINE_SOURCE == 'merge_request_event'
      when: always
    - when: never
  needs: [build, perftest-DGX-A100]
  tags:
    - dgx-a100
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/home/svcnccl/install/openmpi-1.10.7"
    CUDA_HOME: "/project/nccl/toolkits/cuda-${CUDA_VERSION}/"
    NVCC_GENCODE: " -gencode=arch=compute_80,code=sm_80"
    NCCL_CUMEM_ENABLE: "1"
    GPU_APPLICATION_CLOCK: 1410
  script:
    - ./test/scripts/gitlab-runner-build-baseline.sh $CI_MERGE_REQUEST_DIFF_BASE_SHA
    - salloc -N 1 -n 8 --account=coreai_libraries_nccl -pluna -J "coreai_libraries_nccl-cicd::$GITLAB_USER_LOGIN:perftest-single-regression::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t $SINGLE_REGRESSION_SALLOC_TIME ./test/scripts/gitlab-runner-compare-perf.sh $REGRESSION_THRESHOLD $REGRESSION_ITERATIONS $REGRESSION_CHECK_CPU_OVERHEAD $CI_MERGE_REQUEST_DIFF_BASE_SHA
  cache:
    key: "build-${REGRESSION_BASELINE}"
    paths:
      - "build-${REGRESSION_BASELINE}/lib"
      - "build-${REGRESSION_BASELINE}/test/perf"
    key: "build-${CI_MERGE_REQUEST_DIFF_BASE_SHA}"
    paths:
      - "build-${CI_MERGE_REQUEST_DIFF_BASE_SHA}/lib"
      - "build-${CI_MERGE_REQUEST_DIFF_BASE_SHA}/test/perf"
  interruptible: true

perf-regression-multi-DGX-A100:
  stage: perf_regression
  rules:
    - if: $RUN_ON_SELENE == "1" && $CUDA_VERSION != "10.2" && $CI_PIPELINE_SOURCE != "merge_request_event" && $RUN_MULTI_REGRESSION == "1"
      when: always
    - when: never
    - if: $REGRESSION_BASELINE == ""
      variables:
        REGRESSION_BASELINE: "${REGRESSION_BASELINE_TAG}"
  needs: [build, perftest-DGX-A100]
  tags:
    - dgx-a100
  variables:
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/home/svcnccl/install/openmpi-1.10.7"
    CUDA_HOME: "/project/nccl/toolkits/cuda-${CUDA_VERSION}/"
    NVCC_GENCODE: " -gencode=arch=compute_80,code=sm_80"
    NCCL_IB_SL: "1"
    NCCL_CUMEM_ENABLE: "1"
    OMPI_MCA_btl: "^openib"
    OMPI_MCA_btl_tcp_if_include: "enp97s0f1"
    GPU_APPLICATION_CLOCK: 1410
  script:
    - ./test/scripts/gitlab-runner-build-baseline.sh $CI_MERGE_REQUEST_DIFF_BASE_SHA
    - let NUM_PROCESSES=$MULTI_PERFTEST_NODE_COUNT*8 && salloc -N $MULTI_PERFTEST_NODE_COUNT -n $NUM_PROCESSES --account=coreai_libraries_nccl -pluna -J "coreai_libraries_nccl-cicd::$GITLAB_USER_LOGIN:perftest-multi-regression::$CI_COMMIT_BRANCH:$CI_COMMIT_SHORT_SHA" -t $MULTI_REGRESSION_SALLOC_TIME ./test/scripts/gitlab-runner-compare-perf.sh $REGRESSION_THRESHOLD $REGRESSION_ITERATIONS $REGRESSION_CHECK_CPU_OVERHEAD $CI_MERGE_REQUEST_DIFF_BASE_SHA
  cache:
    key: "build-${REGRESSION_BASELINE}"
    paths:
      - "build-${REGRESSION_BASELINE}/lib"
      - "build-${REGRESSION_BASELINE}/test/perf"
    key: "build-${CI_MERGE_REQUEST_DIFF_BASE_SHA}"
    paths:
      - "build-${CI_MERGE_REQUEST_DIFF_BASE_SHA}/lib"
      - "build-${CI_MERGE_REQUEST_DIFF_BASE_SHA}/test/perf"
  interruptible: true

QA-team-ci:
  stage: perf_regression
  rules:
    - when: always
  tags:
    - cqa_host_runner
  script:
    - bash $CI_BUILDS_DIR/CI/ci_perftest.sh
  interruptible: true

replay-multi-gc:
  stage: replay_tests
  rules:
    - when: always
  needs: [build, perftest-multi-gc]
  tags:
    - p100
  variables:
    NCCL_IB_HCA: "mlx5_0"
    NCCL_DEBUG: "WARN"
    MPI_HOME: "/opt/mpi/openmpi-1.10.7"
    CUDA_HOME: "/usr/local/cuda-${CUDA_VERSION}"
    SALLOC_RESNET: "salloc --exclusive -t 00:05:00 -N 2 -n 8 -w gc[01-02]"
    SALLOC_SELF_MULTI: "salloc --exclusive -t 00:05:00 -N 2 -n 16 -w gc[01-02]"
    SALLOC_SELF_SINGLE: "salloc --exclusive -t 00:05:00 -N 1 -n 8 -pP100"
  script:
    - SALLOC=$SALLOC_RESNET ./test/scripts/gitlab-runner-replay-test.sh test/replay/traces/resnet_2x4_trace.txt
    - ./test/scripts/gitlab-runner-replay-self-test.sh
  artifacts:
    when: always
    paths:
      - "**/core.*.*"
    expire_in: 4 hrs
  interruptible: true
